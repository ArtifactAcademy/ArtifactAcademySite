import { createStandardSession } from './session-factory'

export const session05 = createStandardSession({
  id: 'brand-systems',
  number: 5,
  title: 'Brand systems',
  lessonTitles: ['Defining a visual direction', 'Applying repeatable rules'],
  artifactTitle: 'Brand system',
})
