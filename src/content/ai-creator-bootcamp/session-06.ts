import { createStandardSession } from './session-factory'

export const session06 = createStandardSession({
  id: 'motion',
  number: 6,
  title: 'Motion and interaction',
  lessonTitles: ['Motion with purpose', 'Interaction states'],
  artifactTitle: 'Interactive prototype',
})
