import { createStandardSession } from './session-factory'

export const session04 = createStandardSession({
  id: 'components',
  number: 4,
  title: 'Building with components',
  lessonTitles: ['Component thinking', 'Building your layout'],
  artifactTitle: 'Ship a landing section',
})
